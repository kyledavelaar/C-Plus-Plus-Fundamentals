#include <iostream>


int main() {

    // ASSIGNMENT OPERATOR can only be used if object already exists before you assign something to it
    int x = 1;

    // COPY CONSTRUCTOR creates the object and then assigns something to it
    int y{ 3 };


    return 0;
}